apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rss-proxy
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/rss-proxy/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
