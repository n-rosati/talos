apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  interval: 2m
  timeout: 10m
  chart:
    spec:
      chart: jellyfin
      version: 23.9.2
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    jellyfinUpdatePosterImage:
      repository: docker.io/iceshadow/jellyfinupdateposter
      pullPolicy: IfNotPresent
      tag: latest@sha256:2172c6a22452a72e14a194fae184ee534408c23b9e3f4a4a98cd5b145125f6da
    global:
      stopAll: false
    credentials:
      home-s3:
        name: home-s3
        type: s3
        url: ${S3_URL}
        bucket: jellyfin
        encrKey: ${BACKUP_ENCRYPTION_KEY}
        accessKey: ${S3_ACCESS_KEY}
        secretKey: ${S3_SECRET_KEY}
    service:
      jellyfin-up:
        enabled: true
        targetSelector: jellyfin-up
        ports:
          jellyfin-up:
            enabled: true
            primary: true
            port: 8080
            targetPort: 8080
            targetSelector: jellyfin-up
    ingress:
      main:
        enabled: true
        ingressClassName: external
        hosts:
          - host: jellyfin.${DOMAIN_0}
        integrations:
          certManager:
            enabled: true
            certificateIssuer: le-prod
          homepage:
            enabled: true
            group: Media
            href: https://{{ ( index .Values.ingress.main.hosts 0 ).host }}
            widget:
              enabled: true
              type: jellyfin
              url: http://jellyfin.jellyfin.svc.cluster.local:8096
              custom:
                key: ${HOMEPAGE_JELLYFIN_KEY}
    persistence:
      config:
        volsync:
          - credentials: home-s3
            name: config
            type: restic
            dest:
              enabled: true
            src:
              enabled: true
      data:
        volsync:
          - credentials: home-s3
            name: data
            type: restic
            dest:
              enabled: false # CHANGE TO true ON REINSTALL
            src:
              enabled: true
      media:
        enabled: true
        type: nfs
        path: ${NFS_BULK_DATA}/Media
        mountPath: /media
        server: ${TRUENAS_IP}
      jellyfin-update-poster:
        enabled: true
        type: nfs
        path: ${NFS_APP_DATA}/JellyfinUpdatePoster
        server: ${TRUENAS_IP}
        targetSelector:
          jellyfin-up:
            jellyfin-up:
              mountPath: /mount
    autodiscovery:
      enabled: true
    workload:
      main:
        podSpec:
          runtimeClassName: "nvidia"
          containers:
            main:
              resources:
                limits:
                  nvidia.com/gpu: 1
      broadcastproxy:
        enabled: true
      jellyfin-up:
        enabled: false
        type: Deployment
        strategy: RollingUpdate
        replicas: 1
        podSpec:
          terminationGracePeriodSeconds: 10
          containers:
            jellyfin-up:
              primary: true
              enabled: false
              imageSelector: jellyfinUpdatePosterImage
              env:
                JELLYFIN_URL: http://jellyfin.jellyfin.svc.cluster.local:8096
                JELLYFIN_API_KEY: ${JELLYFIN_UPDATE_POSTER_API_KEY}
                TMDB_API_KEY: ${TMDB_API_KEY}
                INCLUDE_EPISODES: true
                ENABLE_WEBHOOK: true
                # SCHEDULED_TIMES: 06:00,12:00,18:00 # Optional: Schedule poster updates at specific times (HH:MM)
                args:
                  - --force
              probes:
                readiness:
                  enabled: false
                liveness:
                  enabled: false
                startup:
                  enabled: false
