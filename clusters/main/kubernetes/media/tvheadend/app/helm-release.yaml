apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tvheadend
  namespace: tvheadend
spec:
  interval: 2m
  timeout: 10m
  chart:
    spec:
      chart: tvheadend
      version: 22.13.7
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    global:
      stopAll: false
    workload:
      main:
        podSpec:
          terminationGracePeriodSeconds: 20
    credentials:
      home-s3:
        name: home-s3
        type: s3
        url: ${S3_URL}
        bucket: tvheadend
        encrKey: ${BACKUP_ENCRYPTION_KEY}
        accessKey: ${S3_ACCESS_KEY}
        secretKey: ${S3_SECRET_KEY}
    service:
      main:
        type: LoadBalancer
        loadBalancerIP: ${TVHEADEND_IP}
        integrations:
          metallb:
            enabled: true
    persistence:
      config:
        volsync:
          - credentials: home-s3
            name: config
            type: restic
            dest:
              enabled: true
            src:
              enabled: true
              trigger:
                schedule: 25 6 * * *
      media:
        enabled: true
        type: nfs
        path: ${NFS_BULK_DATA}/Media
        mountPath: /media
        server: ${TRUENAS_IP}
    addons:
      codeserver:
        enabled: true
        service:
          type: LoadBalancer
        # ingress:
        #   enabled: true
        #   ingressClassName: external
        #   hosts:
        #     - host: edit-iptv.${DOMAIN_0}
        #   integrations:
        #     certManager:
        #     enabled: true
        #     certificateIssuer: le-prod
        #   nginx:
        #     enabled: true
        #     auth:
        #       type: authelia
        #       internalHost: ${AUTHELIA_INTERNAL_DNS}
        #       externalHost: ${AUTHELIA_EXTERNAL_DNS}
